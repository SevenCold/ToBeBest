<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kang.mapper.ItemsCommentsMapper">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.kang.pojo.ItemsCommentsEntity" id="itemsCommentsMap">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="itemId" column="item_id"/>
        <result property="itemName" column="item_name"/>
        <result property="itemSpecId" column="item_spec_id"/>
        <result property="sepcName" column="sepc_name"/>
        <result property="commentLevel" column="comment_level"/>
        <result property="content" column="content"/>
        <result property="createdTime" column="created_time"/>
        <result property="updatedTime" column="updated_time"/>
    </resultMap>

    <select id="getCommentCntByItemId" parameterType="string" resultMap="itemsCommentsMap">
        select count(id) id, comment_level from items_comments WHERE item_id = #{itemId} GROUP BY comment_level
    </select>

    <select id="queryItemComments" parameterType="Map" resultType="com.kang.VO.ItemCommentVO">
        SELECT
        ic.comment_level as commentLevel,
        ic.content as content,
        ic.sepc_name as specName,
        ic.created_time as createdTime,
        u.face as userFace,
        u.nickname as nickname
        FROM
        items_comments ic
        LEFT JOIN
        users u
        ON
        ic.user_id = u.id
        WHERE
        ic.item_id = #{paramsMap.itemId}
        <if test=" paramsMap.level != null and paramsMap.level != '' ">
            AND ic.comment_level = #{paramsMap.level}
        </if>
    </select>

</mapper>